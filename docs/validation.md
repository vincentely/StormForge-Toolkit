# 初始公开仓库验证

本地日期：2026-09-18；上游提交 `984837c730f18459d4e21a86c27f225389deb8c1`。

- 初始目录为空，未覆盖用户文件；本地 Git 初始化 main。
- Node 24.19.0、pnpm 11.19.0；Astro 7.3.3（审计当日 npm latest stable）、TypeScript 6.0.3；依赖精确声明并通过 frozen-lockfile 安装。
- Astro check：0 errors / 0 warnings / 0 hints。
- 单元测试覆盖清单结构、危险地址、轨道分离、文件名、实际文件哈希篡改和 nginx API 路由保持。
- 静态输出：8 个 HTML 页面（含 404）及公开 JSON Schema；117 个内部链接/资源引用通过。
- Playwright 使用本机 Edge 对 1440、390、320 三种宽度检查 7 个用户页面，共 21 项；无横向溢出，无浏览器 pageerror。首页首个 Tab 到跳转正文，Enter 正确定位；200% 根字体无页面横向溢出。
- 截图：`artifacts/visual/desktop-home.png`、`mobile-home.png`，以及其他 6 页各两张；均为实际运行网站截图，保存在忽略目录，未进入 Git。已人工查看首页桌面/手机视图。
- 已跟踪文件通过体积、禁入扩展/目录和常见秘密模式扫描；无上游源码、游戏资源、插件二进制或截图拷贝。本扫描不宣称覆盖所有秘密类型，另有逐项来源审核。
- `git diff --check` 通过。上游只读、未运行核心构建。

复现命令见根 README。包构建命令会生成源码提交标识、字节大小、小写 SHA-256。网站包是静态检查产物，不是核心产品正式发行包。

## 未完成的外部验收

- 用户对网站视觉的明确接受。
- 经授权的真实产品截图；当前网站没有产品截图。
- 插件公开许可、真实正式发行包及产品签名。
- 法定主体、备案、联系信息和完整隐私/用户协议。
- 目标服务器 `nginx -t`、TLS/API/OAuth 回归和生产部署；本地 nginx 文本测试不等于服务器验证。

上述状态见 launch-blockers.md；不得将本地构建成功表述为上线或微信审核通过。
