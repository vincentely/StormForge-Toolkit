# StormForge-Toolkit 维护边界

- 本仓库是 StormForge 公开产品与发行面，不是核心源码仓库。
- `D:\StormForge` 是只读上游事实来源；记录来源提交，不修改上游。
- Editor、Server、Launcher、Client、Runtime 核心二进制仅由私有上游构建、测试、签名和冻结。
- 禁止秘密、第三方游戏原始资产、缓存、数据库、用户数据及私有 Server 实现进入本仓库。
- EXE、DLL、压缩包和构建输出只进入 Releases 或云服务器，禁止进入 Git。
- 官网只能使用核实来源且允许公开的真实产品截图，不得用 AI 图冒充。
- 云服务器是 runtime-only；禁止 npm build、CMake、MSBuild、docker build 或任何源码构建。
- 每个下载必须提供版本、字节大小和小写 SHA-256；禁止假下载、假 latest 和未验证 Release。
- 插件公开内容必须有来源提交、许可证、版本、支持范围和文件清单。所有权迁移前仅为正式 Release 的公开发行快照；禁止脱离上游修改功能或双向同步。
- 缺少法定主体、备案、隐私邮箱等资料时记录阻塞，不编造。
- 生产部署必须得到用户视觉接受，并补齐真实主体资料、目标与凭据及明确部署授权。
- 遵循 KISS、YAGNI、最小改动、失败即报错；不拉取或构建私有核心。
